{% extends 'base.html.twig' %}

{% block title %}Resultado | Los Chapuces{% endblock %}

{% block body %}
<div class="container py-5 text-center">
    
    {% if error == 0 %}
        {# ÉXITO #}
        <div class="card border-success border-2 shadow-lg mx-auto" style="max-width: 600px;">
            <div class="card-body p-5">
                <i class="fas fa-check-circle text-success fa-5x mb-4"></i>
                <h2 class="fw-bold text-success">¡Oído Cocina!</h2>
                <p class="lead">Tu pedido está confirmado.</p>
                <div class="bg-light p-3 rounded border my-4">
                    <strong>Código de recogida:</strong> <span class="fs-4 fw-bold text-dark ms-2">{{ pedido_id }}</span>
                </div>
                <p class="text-muted small">Te hemos enviado un correo (si el servidor quiere).</p>
                <a href="{{ path('categorias') }}" class="btn btn-success btn-lg mt-3">Volver a por más cosas</a>
            </div>
        </div>

    {% elseif error == 1 %}
        {# CESTA VACÍA #}
        <div class="alert alert-warning mx-auto shadow" style="max-width: 600px;">
            <i class="fas fa-exclamation-circle fa-3x mb-3"></i>
            <h3>Pero si no has comprado nada...</h3>
            <p>No puedes hacer un pedido de aire. Echa algo a la cesta primero.</p>
            <a href="{{ path('categorias') }}" class="btn btn-dark mt-2">Ir al almacén</a>
        </div>

    {% elseif error == 2 %}
        {# ERROR BD #}
        <div class="alert alert-danger mx-auto shadow" style="max-width: 600px;">
            <i class="fas fa-bomb fa-3x mb-3"></i>
            <h3>Houston, tenemos un problema</h3>
            <p>Ha petado la base de datos al guardar tu pedido.</p>
            <p class="small">No te preocupes, inténtalo luego o llama a Manolo.</p>
            <a href="{{ path('cesta') }}" class="btn btn-outline-danger mt-2">Volver a intentar</a>
        </div>
    {% endif %}
    
</div>
{% endblock %}