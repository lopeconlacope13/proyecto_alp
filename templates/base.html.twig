<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Mi Tienda Symfony{% endblock %}</title>
        {# Bootstrap 5 CSS #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        {# FontAwesome Icons #}
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
        <style>
            body { background-color: #f8f9fa; }
            .navbar-brand { font-weight: bold; letter-spacing: 1px; }
            .footer { background: #343a40; color: white; padding: 2rem 0; margin-top: 3rem; }
        </style>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
            <div class="container">
                <a class="navbar-brand" href="{{ path('categorias') }}">
                    <i class="fas fa-store me-2"></i>SHOP SYMFONY
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('categorias') }}">Categorías</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link btn btn-outline-light ms-2 position-relative" href="{{ path('cesta') }}">
                                <i class="fas fa-shopping-cart"></i> Mi Cesta
                            </a>
                        </li>
                         {% if app.user %}
                            <li class="nav-item ms-3">
                                <span class="nav-link text-warning">Hola, {{ app.user.userIdentifier }}</span>
                            </li>
                             <li class="nav-item">
                                <a class="nav-link" href="{{ path('logout') }}">Salir</a>
                            </li>
                        {% else %}
                             <li class="nav-item ms-3">
                                <a class="nav-link" href="{{ path('login') }}">Login</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>

        <div class="container main-content" style="min-height: 80vh;">
            {% block body %}{% endblock %}
        </div>

        <footer class="footer text-center">
            <div class="container">
                <p>&copy; {{ "now"|date("Y") }} Shop Symfony. Todos los derechos reservados.</p>
            </div>
        </footer>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>