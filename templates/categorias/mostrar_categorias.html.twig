{% extends 'base.html.twig' %}

{% block title %}Catálogo | Los Chapuces{% endblock %}

{% block body %}
<div class="hero-banner p-5 text-white text-center mb-5">
    <div class="container">
        <h1 class="display-4 fw-bolder">FERRETERÍA "LOS CHAPUCES"</h1>
        <p class="lead fs-4">Si no lo tenemos, es que no te hace falta.</p>
        <p class="small fst-italic text-warning">Expertos en chapuzas domésticas desde ayer por la tarde, el técnico no vale pa na!.</p>
    </div>
</div>

<div class="container mb-5">
    <div class="row align-items-center mb-4 border-bottom pb-2">
        <div class="col">
            <h2 class="fw-bold text-dark"><i class="fas fa-boxes me-2 text-warning"></i>Pasillos</h2>
        </div>
        <div class="col-auto">
            <span class="badge bg-secondary">{{ categorias|length }} Secciones</span>
        </div>
    </div>

    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-4 g-4">
        {% for categoria in categorias %}
            <div class="col">
                <div class="card h-100">
                    <div style="height: 200px; background-color: #f8f9fa; display: flex; align-items: center; justify-content: center; border-bottom: 1px solid #ddd;">
                        <img src="https://placehold.co/300x200/ddd/555?text={{ categoria.nombre|url_encode }}" 
                             class="img-fluid" alt="{{ categoria.nombre }}">
                    </div>
                    <div class="card-body text-center">
                        <h5 class="card-title fw-bold text-uppercase">{{ categoria.nombre }}</h5>
                        <p class="card-text small text-muted">Ref: {{ categoria.codigo }}</p>
                        <a href="{{ path('productos', {'categoria_id': categoria.id}) }}" class="btn btn-primary w-100 rounded-0">
                            Ver Herramientas <i class="fas fa-search ms-1"></i>
                        </a>
                    </div>
                    <div class="card-footer bg-white border-0 text-center">
                         <small class="text-muted">{{ categoria.productos|length }} artículos por ahí tirados</small>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12">
                <div class="alert alert-warning text-center">
                    <h4>El almacén está vacío</h4>
                    <p>Hemos vendido hasta las estanterías.</p>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}